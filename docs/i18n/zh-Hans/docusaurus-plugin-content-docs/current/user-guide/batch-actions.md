---
title: 批量操作
---

# 批量操作

资源列表支持选择多个项目进行批量删除，也可以批量重启符合条件的工作负载。
操作限定在当前集群内，每个条目分别携带命名空间和资源身份。

KubeVision 会单独授权并执行每个条目，返回逐项结果，而不是把整个批次作为一个
事务。出现部分失败时，应先修正权限、冲突或 Kubernetes 校验错误，再只重试
失败项。

批量删除不可撤销。Stateful 工作负载是否保留数据取决于存储和保留策略，确认前
应检查 PVC 所有权和 Finalizer。
