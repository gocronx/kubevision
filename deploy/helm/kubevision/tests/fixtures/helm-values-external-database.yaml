replicaCount: 2

deploymentStrategy:
  type: RollingUpdate

persistence:
  enabled: false

existingSecret: kubevision-runtime-secrets

config:
  database:
    driver: postgres
    dsn: "postgres://kubevision@example.invalid/kubevision"
